# AI Savior Firefox Extension

Build output for the Firefox variant of AI Savior.

The source code is shared with `chrome_extension/src`. Build this variant from the repo root:

```bash
npm run build:firefox
```

Then load `firefox_extension/dist` from `about:debugging#/runtime/this-firefox`.
