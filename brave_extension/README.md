# AI Savior Brave Extension

Build output for the Brave variant of AI Savior.

The source code is shared with `chrome_extension/src`. Build this variant from the repo root:

```bash
npm run build:brave
```

Then load `brave_extension/dist` from `brave://extensions/` with developer mode enabled.
